<h1>Genres</h1>
<ul>
  <% @genres.each do |genre| %>
    <li><%= link_to genre.name, genre_path(genre) %></li>
  <% end %>
</ul>
<section class="admin">
  <%= link_to 'Create Genre', new_genre_path, { class: "button" } %>
</section>